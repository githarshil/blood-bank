{
  "name": "blood-bank-backend",
  "version": "1.0.0",
  "description": "Blood Bank & Donor Management System - Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:frontend": "cd blood-bank-frontend && npm install && npm run build",
    "build:all": "npm install && npm run build:frontend",
    "vercel-build": "cd blood-bank-frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "blood-bank",
    "donor-management",
    "medical",
    "express",
    "mysql"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
